Sources for the plugin `plugin.appodeal.Flurry`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.Flurry" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
